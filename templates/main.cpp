#include "prettyprint.hpp"

#include <cstddef>
#include <cstdint>
#include <iostream>

int main() {
  std::size_t testCases;
  std::cin >> testCases;

  for (std::size_t t = 0; t < testCases; ++t) {

    std::cout << t + 1 << ' '
              << ;
  }

  return 0;
}
