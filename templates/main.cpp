#include "prettyprint.hpp"

#include <cstddef>
#include <cstdint>
#include <iostream>
#include <vector>
#include <string>
#include <numeric>
#include <algorithm>
#include <bitset>
#include <cmath>
#include <unistd.h> 

int main() {
  std::size_t testCases;
  std::cin >> testCases;

  for (std::size_t t = 0; t < testCases; ++t) {

    std::cout << t + 1 << ' '
              << todo << '\n';
  }

  return 0;
}
